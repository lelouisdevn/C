#include <stdio.h>

int main(){
    // int value = 21;
    int *pointer = NULL;

    // pointer = &value;

    // printf ("Address of variable \"value\": %x\n", &value);
    // printf ("Address of variable \"value\" stored in \"pointer\": %x\n", pointer);
    // printf ("Value of the variale which pointer store: %d", *pointer);

    printf ("%d\n", (int) pointer);
}