#include "iostream"
using namespace std;

int main() {
    freopen("dothi.txt","r",stdin);
    int n, m;
    cin >> n >> m;
    cout << n << ": " << m;
    // scanf("%d%d", &n, &m);
    // printf("%d %d", n, m);
}